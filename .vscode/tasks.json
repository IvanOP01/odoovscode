{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker Compose Debug",
            "type": "shell",
            "command": "docker compose down && docker compose up -d --build && sleep 20"
        }
    ]
}